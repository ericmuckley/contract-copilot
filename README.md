# Contract Copilot

AI copilot that helps review contracts and estimate pricing.

## Running locally

1. **Set environment variables**: Copy the `.env.example` file, rename to `.env`, and populate with proper variables.
1. **Install dependencies**: `npm i`
1. **Run local development server**: `npm run dev -- --open`
